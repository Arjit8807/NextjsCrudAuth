// src/lib/db.ts
// This file is a shared utility for your backend.

// We declare a global Map to store user data.
// `export` makes this Map accessible to other files.
// `const` ensures that this Map variable cannot be reassigned.
export const users = new Map();